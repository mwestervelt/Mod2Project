<h1>Add ❤️ to Resolution:</h1>

<%= form_for @heart do |f| %>
  <%= f.label :resolution %>
  <%= f.collection_select :resolution_id, Resolution.all, :id, :title %>
  <%= f.hidden_field :user_id, value: session[:signed_in_user_id] %>  
  <%= f.submit "❤️" %>
  <% end %>
